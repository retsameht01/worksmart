//
//  ScratchFile.swift
//  worksmart
//
//  Created by Tin Le on 3/16/20.
//  Copyright © 2020 Tin Le. All rights reserved.
//

import Foundation
/*
THis will set the background for the screen
ZStack {
    CustomColor.cleanBlue.edgesIgnoringSafeArea(.all)
    
    VStack{
        Text("Test")
            .foregroundColor(.)
    }
}

VStack(alignment:.leading) {
    Text(getTodayDate())
    .font(Font.system(size: 30.0))
        .fontWeight(.black)
        .padding(.bottom, 20)
        .foregroundColor(.blue)
    
    VStack {
        ListItemWithRoundCornerBG(text: "Katie Smith @ 1:34")
        ListItemWithRoundCornerBG(text: "Lisa Jang @ 1:34")
    }
    .background(Color(red: 239.0/255.0, green: 243.0/255.0, blue: 244.0/255.0, opacity: 1.0))
    .cornerRadius(20.0)
    
}

.background(CustomColor.pastelLightBlue)
*/

//Loop through the events

      /*if let events = events {
          let section = eventDates[section]
          var count = 0
          for event in events {
                  if let date = event.occurrenceDate {
                      if (date.description == section) {
                          count = count + 1
                      }
              }
          }
          //print("There are \(count) for \(section)")
          return count
      }*/
